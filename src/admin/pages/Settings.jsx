export default function Settings() {
  return (
    <div>
      <h1>Settings</h1>
      <p style={{marginTop:8,opacity:.8}}>
        Put system configuration here (branding, email, feature flags, etc.).
      </p>
    </div>
  );
}
